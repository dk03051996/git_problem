git remote -v # to check our local repo is connected to which repo
git log dev.   # it will tell the logs of dev branch irrespective of which branch we are
git log --oneline.   # to check the commit with commit id and commit message only
